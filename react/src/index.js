import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import DevStore from '../src/pages'

ReactDOM.render(
  <React.StrictMode>
    <DevStore />
  </React.StrictMode>,
  document.getElementById('root')
);
